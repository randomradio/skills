interface:
  display_name: "RandomRadio Upgrade"
  short_description: "Upgrade all installable RandomRadio-managed skills from GitHub."
  default_prompt: "Use $randomradio-upgrade to refresh local RandomRadio skills to the latest repository version, upgrading every installable managed skill by default unless the user requests a subset."
